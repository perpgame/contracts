[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.28"
optimizer = true
# AgentTreasury is close to EIP-170's 24,576-byte runtime-code limit. Favor
# deployable bytecode over marginal execution-gas savings for this deployment.
optimizer_runs = 0
via_ir = true
fs_permissions = [{ access = "read", path = "./" }]
gas_reports = ["AgentTreasury", "AgentCurve"]

[rpc_endpoints]
hyperevm = "${HYPEREVM_RPC_URL}"
hyperevm_testnet = "${HYPEREVM_TESTNET_RPC_URL}"

[etherscan]
hyperevm = { key = "${HYPEREVM_VERIFIER_API_KEY}", url = "${HYPEREVM_VERIFIER_URL}" }
